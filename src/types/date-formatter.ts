export type DateFormatter = {
  weekday: string;
  month: string;
  day: number;
  hour: number;
  minutes: string | number;
};

export type MonthKeys = { [key: number]: string };

export type WeekdayKeys = { [key: number]: string };
